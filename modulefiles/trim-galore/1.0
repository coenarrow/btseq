#%Module1.0
proc ModulesHelp { } {
    puts stderr "trim-galore 1.0"
}
module-whatis "Loads the trim-galore tool"

# Set up the environment (path to the Conda environment)
set root /group/pgh004/carrow/repo/btseq/modules

prepend-path PATH $root/bin
